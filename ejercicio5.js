/*
5.- Crear una función que reciba como parámetro un string y que devuelva ese mismo
string con la primera letra convertida a mayúscula.
*/

function palabra(a){
    return a = a.toUpperCase();
}

alert(palabra('mermelada'));